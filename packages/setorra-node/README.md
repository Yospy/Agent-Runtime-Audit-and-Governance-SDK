# Setorra Node Package Placeholder

This package directory is intentionally reserved and contains no SDK implementation.

Use `packages/setorra-ai` for the active TypeScript SDK. Keep this placeholder until package naming is finalized, then either remove the directory or migrate the TypeScript SDK here in a dedicated package-renaming sprint.
