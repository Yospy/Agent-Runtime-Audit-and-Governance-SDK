{
  "version": "2025-10-30",
  "metadata": {
    "org_id": "acme-co",
    "owner": "enterprise-security",
    "description": "ACME policy: deny deletes; require approval for external attendees on create; allow view"
  },
  "sdk": {
    "decision": "allow",
    "fail_mode": "fail-open"
  },
  "defaults": {
    "decision": "allow",
    "fail_mode": "fail-closed"
  },
  "actions": {
    "agent.invoke": {
      "default_decision": "allow",
      "fail_mode": "fail-open",
      "rules": []
    },
    "calendar.view": {
      "default_decision": "allow",
      "fail_mode": "fail-closed",
      "rules": []
    },
    "calendar.delete": {
      "default_decision": "deny",
      "fail_mode": "fail-closed",
      "rules": []
    },
    "calendar.update": {
      "default_decision": "allow",
      "fail_mode": "fail-closed",
      "rules": []
    },
    "calendar.create": {
      "default_decision": "allow",
      "fail_mode": "fail-closed",
      "rules": [
        {
          "id": "ext-attendee-external-domain",
          "decision": "require_approval",
          "match": {
            "parameters.arguments.attendees": { "contains": "@gmail.com" }
          },
          "approval": {
            "requires_token": true,
            "expires_in": 900,
            "metadata": {
              "reason": "external_attendee",
              "approver_email_required": true
            }
          },
          "rationale": "External attendee requires explicit approval"
        }
      ]
    }
  }
}

